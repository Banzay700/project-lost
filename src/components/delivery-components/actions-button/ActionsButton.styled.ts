import { Stack, styled } from '@mui/material'

export const ActionsButtonWrapper = styled(Stack)(() => ({
  borderRadius: '16px 16px 0 0',
  padding: '20px',
  boxShadow: '0px -10px 24px 0px rgba(228, 228, 228, 0.5)',
  alignItems: 'center',
  gap: '10px',
}))
